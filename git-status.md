# Git Status

El comando git status muestra archivos que aun no son trackeados por git y archivos que han sido modificados. De esta forma se puede ver que archivos falta agregar a la staging area mediante el comando git add, y qué archivos modificados se pueden confirmar mediante git commit. 